# Editor / OS
.DS_Store
.vscode/.server-controller-port.log

# Private planning docs (NLnet proposal, project roadmap, TLA+ POC, grant context).
# Lives locally in grace-commons/grants/ for convenience; not committed.
grants/

# TLC model-checker on-disk metadata (created on every CLI run; not a spec artifact).
compositions/states/

# TLC-adapter output: derived camelCase copies of kebab-case .tla models for standard
# TLC (tools/harness/tla-adapter.mjs). Derived on demand, never committed.
build-tlc/

# Term-adapter output: derived Term->casing manifests projected from each page's
# Terms cards (tools/harness/term-adapter.mjs). Derived on demand, never committed —
# the canonical token lives on the spec page's card `Projects:` line.
build-terms/

# Demos — committed source, but exclude build artifacts and runtime data.
demos/*/node_modules/
demos/*/.deno/
demos/*/data/
demos/*/public/styles.css

# Python bytecode
__pycache__/
*.pyc

# virtiofs rename-on-unlink orphans (sandbox artifact)
.fuse_hidden*

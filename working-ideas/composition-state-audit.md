# Composition-state audit matrix — provisional (2026-06-10)

> **Status: internal staging, not canonical.** Provisional classification of every grounded composition's Application-state elements against the composition-state rule adjudicated 2026-06-10 (`execution-contract.md` section Composition state: derivable → derived index; non-derivable → extracted atom; history-shaped memory → compose Event Log). Produced in the Refactor 1 session from the specs' own Application-state text, **without** per-composition audit rounds — every verdict below except the two settled poles is provisional until the audit wave's touch-triggered re-pass folds the classification into the owning spec. Tracked as ROADMAP methodology debt #9. This file dies into the patterns' Application state subsections and Lineage notes as the folds land.

Verdict vocabulary:

- **derived-index** — every fact rebuildable from constituent stores through declared read surfaces; the fold names the rebuild procedure.
- **extraction-pending** — carries truth no constituent store holds; the fold flags it and names the proposed atom.
- **re-house-on-Event-Log** — history-shaped memory (attempt logs, lifecycle journals) that the Contract's existing rule already assigns to a composed Event Log rather than a composition store.
- **audit-needed** — the classification turns on a fact the spec text alone does not settle (usually: whether the corresponding substrate/Event-Log write is atomic with the truth it would replay, or whether event `data` payloads carry the fields the rebuild needs). Lean recorded.
- **none / conforming** — no stored Application state (pure derived queries, or explicit None).

| # | Composition | Element | Provisional verdict | Basis (one line) |
|---|---|---|---|---|
| 1 | Idempotent Reservation | `token_results` | **extraction-pending → Idempotency Result Memo** (settled pole) | token → result is new truth; no replay of PC + DP reproduces it; DP's spec disclaims acting on results. ROADMAP proposal opened 2026-06-10. |
| 2 | Immutable Transaction Ledger (C6) | `disclosure_to_event` | **derived-index — FOLDED 2026-06-10** (C6 batch round, Final Critique 6) | Binding fact lives in `ledger.disclosed` event `data.disclosure_id`; rebuild = enumerate `ledger.disclosed` events. Folded into C6's Application state with all three rule obligations discharged; the TLA+ model now omits the index per Contract obligation 2. One derivability boundary recorded: a lawful purge of a `ledger.disclosed` event destroys the binding fact (rebuild drops the entry; `binding-purged` decidable via the purge-preserved accounting key). |
| 3 | Reservation Lifecycle (C9) | `reservation_to_pool` | audit-needed (lean derived-index) | Every state-changing action appends an Event Log entry carrying reservation id + pool id; derivable iff the append is atomic with the action. |
| 4 | Reservation Lifecycle (C9) | `slot_released` flag | **audit-needed — the named mixed case** | Compensation truth ("slot returned exactly once," Invariant 2). Derivable from EL replay only if release + append commit atomically; otherwise the flag carries truth the log lacks. Run early; this verdict shapes the rubric. |
| 5 | Reservation Lifecycle (C9) | `token_results` | extraction-pending → Idempotency Result Memo | Inherited IR case verbatim. |
| 6 | Audit Trail | `event_to_attestation` | audit-needed (lean derived-index) | "Lets the auditor traverse … without scanning" is index language; derivable if attestation records carry event linkage. |
| 7 | Audit Trail | `event_to_retention` | audit-needed (lean derived-index) | Same shape; derivable if Retention Window records carry the event ref. |
| 8 | Audit Trail | `seal_coverage` | audit-needed (lean derived-index) | Derivable if each seal evidence record commits to its own range (it should — that is what a seal is). |
| 9 | Audit Trail | `sealed_through` | derived-index (lean) | Max over `seal_coverage` ranges; pure aggregate. |
| 10 | Multi-Party Approval (C4) | `chain_store` | **audit-needed — extraction candidate** | A record store with its own state machine (chain states, quorum_rule, approver_set). Replay-derivable only if `initiate_chain`/decision/`chain_resolved` audit events carry the full chain definition and the quorum evaluation is deterministic over step records; otherwise this is the corpus's strongest extraction candidate (an Approval Chain atom). |
| 11 | Multi-Party Approval (C4) | `chain_to_steps`, `step_to_chain`, `step_to_assignment` | derived-index (lean) | Traversal indexes over chain/step/assignment records. |
| 12 | Multi-Party Approval (C4) | `chain_terminal_at` | derived-index (lean) | Derivable from `chain_resolved` events / deterministic quorum replay. |
| 13 | Privileged Access Provisioning (C5) | `request_store` | audit-needed — same class as C4 `chain_store` | Request records with lifecycle states; lean replay-derivable from the single Audit Trail instance's events if their `data` carries the request definition (incl. `expires_at`). |
| 14 | Privileged Access Provisioning (C5) | `request_to_chain`, `request_to_capability`, `capability_to_request` | derived-index (lean) | One-to-one bindings; provisioning events carry both ids. |
| 15 | Privileged Access Provisioning (C5) | `session_access_log` | derived-index (lean) | `access_exercised` events are already recorded in the Audit Trail instance; the log looks like an EL filter. Confirm nothing extra (failed-before-redeem attempts?) lives only here; else re-house-on-Event-Log. |
| 16 | Defensible Retention (C1) | `record_to_retentions`, `retention_to_record` | derived-index (lean) | Both directions are queries over the Retention Window store (retention_id ↔ record_ref, retention_until). |
| 17 | Consent & Preference Mgmt (C2) | `consent_to_downstream` | **audit-needed (lean extraction-pending or re-house)** | The downstream-binding registry reads as the record of record for `register_processing` — truth no constituent holds at registration time. `withdraw_consent`'s complete-enumeration obligation consumes it. If registration events carry the binding, replay-derivable; else this is a registry concern needing a home. |
| 18 | Consent & Preference Mgmt (C2) | `consent_to_retention` | derived-index (lean) | Retention Window store query by record_ref = consent record. |
| 19 | Consent & Preference Mgmt (C2) | `consent_to_subject_purpose` | derived-index | Spec calls it a convenience index; Consent store holds subject/purpose. |
| 20 | DSAR (C7) | `request_to_subject` | audit-needed (lean: routes to the seeded Completeness Model atom) | Intake truth exists before any constituent write (between `receive_request` and fulfillment the map is the only home). C7 already seeds Completeness Model — request/case identity likely belongs there. |
| 21 | DSAR (C7) | `request_to_fulfillment` | derived-index (lean) | The sealed `dsar.*_fulfilled` event carries the complete disposition set; rebuild = enumerate fulfillment events (C6-symmetric). |
| 22 | KYC (C8) | `case_to_monitoring` | audit-needed (lean derived-index) | `next_review_due` advanced by recorded actions; derivable if each advance's event carries the new due date. |
| 23 | KYC (C8) | `party_to_kyc_case` | **audit-needed** | Load-bearing gate truth (verified-through-C8 reads this index). Replay-derivable iff case open/close/enrollment-path all land in event `data`. The gate consuming it raises the consistency-claim question (rule obligation 3) even if derivable. |
| 24 | KYC (C8) | `case_to_retentions` | derived-index (lean) | Two Retention Window placements; RW store query. |
| 25 | KYC (C8) | `case_to_open_triggers` | derived-index (lean) | Spec claims a records-alone `trigger_id` lifecycle; rebuild = replay trigger events. |
| 26 | Login (C13) | `credential_to_sessions`, `session_to_credential` | audit-needed (lean derived-index) | Derivable if `login_succeeded` event data carries (credential_id, session_token) and cascade events carry revocations; the cascade gate consumes it, so the consistency-claim question applies. |
| 27 | Login (C13) | `login_event_log` | re-house-on-Event-Log / derived-index (lean) | `login` already writes `login_succeeded`/`login_failed` to the substrate; if every entry here mirrors an AT event, this is an EL filter, not a store. |
| 28 | Authenticated Actor (C17) | `principal_to_actor`, `actor_to_principal` | **audit-needed — extraction candidate** | The namespace bijection is registered truth (bound_at, binding) that precedes any attestation; no AT substrate in the cut, and the attest log is not its source. Candidate homes: a registry-shaped atom (a *Binding Registry*), or an Event-Log-backed registration journal. C17's load-bearing claim rides on it — run with care. **At fold, run the A5 composition-layer extraction gate** (does the binding recur at the invariant level across compositions? own invariant surface? survives Jackson's wiring-not-primitive rule?) before minting an atom; and note C17 is *grounded*, so any extraction is a touch-triggered refactor with a constituent cascade, not a free add. |
| 29 | Authenticated Actor (C17) | `attest_log` | re-house-on-Event-Log (lean) | Append-only attempt journal incl. refusals; exactly the compose-Event-Log rule (no AT substrate in C17's cut, so the journal currently has no constituent home). |
| 30 | Actor Suspension (C18) | `actor_suspension_state` | audit-needed (lean derived-index) | Composition-introduced lifecycle; transitions write sealed `actor.suspended`/`actor.reinstated` events inside the all-or-nothing transaction → replay-derivable if no-ops/failures don't gate on un-replayable truth. |
| 31 | Actor Suspension (C18) | `suspension_log` | re-house-on-Event-Log / audit-needed | Records no-op and failed calls that produce no substrate event; truth only this log holds. Either those attempt records ride a composed Event Log or they are out-of-band observability, not spec state. |
| 32 | Attributed Permissions Admin | `grant_attribution`, `revocation_attribution` | audit-needed (lean derived-index) | Derivable if Actor Identity attestation payloads name the grant_id (the pair is written by one composition action). Alloy model touches these maps — re-pass implications. |
| 33 | Capability-Backed Sharing (C15) | `capability_to_sharing` | derived-index (lean) | Fields live in the Capability record (immutable allocator_ref) + `sharing.authorized` event data. |
| 34 | Capability-Backed Sharing (C15) | `disclosure_to_redemption` | derived-index (lean) | `sharing.disclosed` event data carries capability_token + allocator_ref (C6-symmetric binding). |
| 35 | Chain of Custody (C12) | `entry_to_event` | derived-index (lean) | C6-symmetric: custody audit events carry the Provenance entry_id. |
| 36 | Forensic Recovery (C3) | `record_to_events` | derived-index (lean) | Rebuild = substrate Event Log filtered by record_id, in order; the spec already calls the EL authoritative versus Soft Delete's current-state-only summary. |
| 37 | Stateful Workflow Execution (C10) | `workflow_store` | audit-needed (lean derived-index) | Instance state is the constituent's (W/SM); the store's extra fields (subject_ref, …) derivable if `workflow_started` event data carries them. |
| 38 | Stateful Workflow Execution (C10) | `gate_binding`, `gate_to_assignment` | derived-index (lean) | `gate_opened` events carry (instance, action, step); assignment binding in event data. |
| 39 | Stateful Workflow Execution (C10) | `transition_to_event` | derived-index (lean) | C6/C12/C3-symmetric binding map. |
| 40 | Shared Todo | *(derived queries only)* | none / conforming | `responsible_actor`, `visible_tasks` are pure joins over constituent Q surfaces; no stored Application state. |
| 41 | Undo History | Personal-Todo-shaped replay state | derived-index by construction (confirm not materialized-with-claims) | Visible state = replay of non-undone Event Log events; the canonical derived idealization. Confirm no consistency claim that a rebuild can't honor. |
| 42 | Notification Fanout | *(explicit None)* | none / conforming | Spec states None; per-recipient state lives in Delivery Task atoms. |
| 43 | External Onboarding (C16) | *(no Application state subsection)* | none / conforming (confirm in re-pass) | No emergent stored state in the spec; step-order gate is evaluated in-action. |
| 44 | Session-Gated Authorization (C14) | *(no Application state subsection)* | none / conforming (confirm in re-pass) | Principal binding evaluated per call; no stored maps. |

## Reading the matrix

Counts (provisional): 2 settled poles; ~19 lean derived-index; 3 strong audit cases that will shape the rubric (C9 `slot_released`; C4 `chain_store`; C17 binding maps); 4 re-house-on-Event-Log candidates (attempt/lifecycle journals); 4 none/conforming. The recurring decisive fact is the same almost everywhere: **does the event/audit write commit atomically with the truth it would replay, and does its `data` carry the fields a rebuild needs?** Where yes, the element is a derived index and the fold is cheap. Where no, the element carries truth and the choice is extraction vs. re-housing vs. tightening the event payload — a per-pattern decision for the audit round, not this matrix.

Order for the audit wave (per the Refactor 1 handoff): ~~C6 (settled, cheapest fold, also rides the open Refactor 1 finding batch)~~ **C6 folded 2026-06-10**, IR (settled, pairs with the Memo proposal), C9 (the mixed case), then C4/C17 (the hard cases), then the long derivable tail. Note for the C12/C3 rows (35, 36): the C6 batch round's mirror check routed the two Invariant-4 coverage-gap findings (recorded in [`tools/harness/coverage/chain-of-custody.md`](../tools/harness/coverage/chain-of-custody.md) and [`tools/harness/coverage/forensic-recovery.md`](../tools/harness/coverage/forensic-recovery.md); the Refactor 1 staging handoff that first logged them is dispersed and deleted) — their binding maps sit inside their models' atomicity surfaces, so their derived-index folds and their model fixes should ride the same round, with C6's 2026-06-10 revision as the template.

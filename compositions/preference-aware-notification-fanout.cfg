SPECIFICATION Spec
INVARIANT Safety
CHECK_DEADLOCK FALSE
CONSTANTS
    Cap = 2
    Workers = {w1, w2, w3}
\* Bound saturation (recorded per pressure-testing.md section Formal-model authoring
\* pitfalls): the state space is exhaustively explored at each constant choice;
\* the bound-bump check raises Workers by one and confirms Inv4_CapSafety still
\* holds with the state count recorded below, so the chosen bound is deliberate.
\*   Cap = 2, Workers = {w1,w2,w3}      -> 10 states, exhaustive, holds (canonical run)
\*   Cap = 2, Workers = {w1,w2,w3,w4}   -> 29 states, exhaustive, holds (bump run)
\* Workers = Cap + 1 is the minimal configuration that makes the suppression
\* branch reachable (a worker must find the cap already met) and gives the
\* buggy twin its overshoot (two observers on the last slot).

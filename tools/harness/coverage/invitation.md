# Coverage matrix — `invitation`

- **Pattern:** `atoms/invitation.md`
- **Model:** `invitation.tla` (TLA+ state-machine) + two isolated buggy twins — `invitation-buggy.tla` (single-resolution) and `invitation-buggy-derivation.tla` (positive read-time derivation, FC F2).
- **Reviewer / date:** regenerated 2026-06-23 against the post-refactor model (execution/render-time refactor, 2026-06-21; `now`/`id_t` reverted to pipeline-injected per the Final Critique council, 2026-06-23). Supersedes the 2026-06-03 matrix, which described the pre-refactor model (stored `Expired`, an `Expire` action, four reachable terminal states, no clock variable, and an over-claimed single-resolution-as-concurrency reading).
- **Formal-layer vote load-bearing claims:** Invariant 2 (single-resolution — at most one stored-terminal write, immutable thereafter) and Invariant 12 (expiry is derived, never written — the refactor's load-bearing render-time guarantee).

## Step 0 — model shape (post-refactor)

`Expired` is no longer a stored status. The stored `state` enum is `{Pending, Accepted, Declined, Revoked}`; `Expired` is a **derived read-time projection** (`Lapsed(c) ≜ state = Pending ∧ c ≥ ExpiresAt`, surfaced as `EffStatus(c)`) evaluated against the injected clock `now`. There is no `Expire` action — lapse never writes. The three stored writes are `Accept`, `Decline`, `Revoke`; each guards on `state = Pending ∧ now < ExpiresAt`. A ghost `resolution` records the first stored terminal reached, so single-resolution is the falsifiable predicate `resolution # none ⇒ state = resolution`. The clock and token are pipeline-injected at the I/O seam (not action parameters); the model carries `now` as an advancing clock variable and reads it in the pure resolving guards and in `EffStatus`.

## Step 1 — harness run (must pass)

- Correct model: `node tools/harness/check.mjs atoms/invitation.tla` → `PASS` ✓ — **16 states**, all five invariants hold (`Inv_SingleResolution`, `Inv_NoStoredExpired`, `Inv_DerivedExpiryCoherent`, `Inv_LapsedReadsExpired`, `TypeOK`). Verified 2026-06-23.
- Buggy twin (single-resolution): `node tools/harness/check.mjs atoms/invitation-buggy.tla --buggy` → `PASS` (rejected) ✓ — `Inv_SingleResolution` violated at **7 states** (`Decline` → `AcceptBuggy` drops the `state = Pending` guard → `state = Accepted` while `resolution = Declined`). The derivation is left correct in this twin, so it isolates the single-resolution defect.
- Buggy twin (derivation, FC F2): `node tools/harness/check.mjs atoms/invitation-buggy-derivation.tla --buggy` → `PASS` (rejected) ✓ — `Inv_LapsedReadsExpired` violated at **9 states** (`BrokenEffStatus` echoes the stored `state` and never returns `"Expired"`, so a lapsed Pending record reads `"Pending"`). Single-resolution is left correct in this twin, so it isolates the read-time derivation defect. This twin demonstrates that the new positive-derivation check actually bites — the FC F2 finding (Inv 12 asserted only vacuously) is closed.

## Step 2 — coverage matrix

One row per numbered invariant, Invariant 1 through 12.

| Spec invariant (no. + name) | Load-bearing (vote)? | Verdict | Model construct / reason |
|---|---|---|---|
| Invariant 1 — Initiation immutability | no | by-construction | `invitation_token`, `inviter_ref`, `invitee_ref`, `context`, `initiated_at`, `expires_at` are not modeled as state variables (the model abstracts to `state`, `resolution`, `now`); no action mutates them. Mutation is structurally absent. Resolution-field immutability is captured indirectly by Invariant 2's `resolution` ghost. |
| Invariant 2 — Single-resolution (by write) | YES | **covered** (post-write immutability) | `Inv_SingleResolution == (resolution # "none") => (state = resolution)` — asserted directly. Each resolving action (`Accept`, `Decline`, `Revoke`) guards on `state = "Pending"`, so the first interleaved winner resolves and every later attempt is disabled (the `already-resolved` rejection); the ghost `resolution` pins the first terminal and the predicate catches any overwrite. The `invitation-buggy.tla` twin (drops the Pending guard) is rejected on exactly this invariant. **What is verified is post-write immutability** — once a stored terminal is written, no second terminal can replace it. The *check-and-commit atomicity under contention* half (two writers racing from Pending) is NOT verified here — see the out-of-scope residual below. |
| Invariant 3 — Acceptance binds identity | no | out-of-scope (named) | `accepting_identity_ref` is not modeled — "NOT MODELED: identity binding at accept, field validation." A field-presence property at the write boundary, not a state-machine safety claim. |
| Invariant 4 — Opaque invitee at initiation | no | out-of-scope (named) | `invitee_ref`/`accepting_identity_ref` are field-level references not modeled; "the atom never validates one against the other" is a structural non-action, not a transition property. |
| Invariant 5 — Three structurally distinct stored terminals; `Expired` is derived | no | by-construction (stored half) + **covered** (derived half) | `StoredTerminals == {"Accepted","Declined","Revoked"}` are three distinct values `state` may take (distinctness is definitional). The per-terminal field patterns (`accepted_at`, `declined_at`, revocation fields) are not modeled (field-level). The "`Expired` is derived, not a stored value" half is covered by `Inv_NoStoredExpired` (`state ∈ StoredStates`) — the store never holds an `Expired` value. |
| Invariant 6 — `already-resolved` carries the stored terminal; lapse is `expired` | no | out-of-scope (named) | Rejection-payload shape, observable at the API boundary, not a state-machine state predicate. The structural separation it rests on (a lapsed record is never a stored terminal) is covered transitively: resolving writes are disabled once `now ≥ ExpiresAt`, so a lapsed record stays `Pending` and reads `Expired` (`Inv_LapsedReadsExpired`), never `already-resolved(Expired)`. |
| Invariant 7 — Expiry deadline immutability | no | by-construction | `ExpiresAt` is a fixed CONSTANT, not a variable; no action mutates it. Immutability is structural. |
| Invariant 8 — Revocation attribution completeness | no | out-of-scope (named) | `revoked_at`/`revoked_by_ref`/`revocation_reason` are attribution fields not modeled — "NOT MODELED: field validation." |
| Invariant 9 — Every invitation has a finite lifetime | no | by-construction | `ExpiresAt` is always present (a CONSTANT natural); there is no "null deadline" representable. The derived-expiry machinery (`Lapsed`/`EffStatus`) presupposes it. |
| Invariant 10 — Invitation durability | no | out-of-scope (named) | Storage durability and the no-partial-record guarantee are host/store obligations, not a state-machine property at this abstraction. |
| Invariant 11 — Token uniqueness | no | out-of-scope (named) | Token id discipline is Alloy-class; a single invitation is modeled here, so uniqueness has no two-record witness to assert. "NOT MODELED: token id discipline / uniqueness." |
| Invariant 12 — Expiry is derived, never written | YES | **covered** | Three constructs. `Inv_NoStoredExpired` (`state ∈ StoredStates`) — the store never holds an `Expired` value (catches a future edit re-introducing a stored status). `Inv_DerivedExpiryCoherent` (`state ∈ StoredTerminals ⇒ EffStatus(now) = state`) — the NEGATIVE half: a written terminal always reads back as itself. `Inv_LapsedReadsExpired` (`Lapsed(now) ⇒ EffStatus(now) = "Expired"`) — the POSITIVE half, **non-vacuous** (its antecedent is reachable: every Pending state at `now ≥ ExpiresAt` satisfies it). The `invitation-buggy-derivation.tla` twin breaks `EffStatus` and is rejected on `Inv_LapsedReadsExpired`. The "lapse writes nothing" half is by-construction: there is no `Expire` action, so no write can fire on lapse. |

Out-of-scope residual, named: the **concurrent check-and-commit interleaving** (Invariant 2's two-writers-racing-from-Pending atomicity) is verified here only as **post-write immutability** (resolution never changes once set), not as runtime serialization of the check-and-commit window. The atomicity of "read `status = Pending`, then write the terminal" under genuine contention is a **runtime-serialization obligation** — the Execution Contract's sequence-safety class — discharged by the host store (a conditional / compare-and-set write or equivalent serialization), not by the atom's English or this state-machine model. It is recorded here as **by-construction / out-of-scope**, NOT claimed as covered concurrency. (TLA+ interleaving of the two enabled resolving actions does establish *that two actions cannot both win*, since each is disabled once `state ≠ Pending`; what is out-of-scope is the sub-action TOCTOU window between the guard read and the commit write, which is below this model's atomic-action granularity.) Where check-and-commit-under-contention is load-bearing it is a host obligation; composing patterns that serialize onboarding (External Onboarding, C16) inherit it from the contract.

## Step 3 — bound saturation

- **Saturation point: 16 states at `ExpiresAt = 2`, for any `MaxClock ≥ ExpiresAt + 1`.** `Tick` is clamped at `ExpiresAt + 1` (FC F1), so the reachable clock values are `{0, 1, 2, 3}` and exploration saturates there regardless of `MaxClock`. Verified: the model holds at **16 states** for `MaxClock ∈ {3, 6, 10, 20}` — raising the domain bound adds zero reachable states. (Pre-fix, with `Tick` guarded by `MaxClock`, the count grew without bound: 16 → 28 → 44 states at `MaxClock = 3 → 6 → 10`. That unbounded growth was the FC F1 defect; the clamp fixes it.) The 16 states decompose as 4 Pending states (`now ∈ {0,1,2,3}`) + 12 resolved states (3 stored terminals × 4 reachable clock values each). One tick past the deadline (`now = ExpiresAt + 1`) is the only behaviorally-distinct lapsed clock value — every clock value `≥ ExpiresAt` produces identical guard outcomes — so the clamp loses no reachable behavior. The canonical `invitation.cfg` sets `MaxClock = 6` (> the clamp) precisely to demonstrate the headroom adds nothing.

## Outcome

- GAP rows: none.
- by-construction flags on load-bearing invariants: Invariant 12's "lapse writes nothing" half (no `Expire` action exists). Invariant 2 is directly asserted and twin-rejected; its concurrent check-and-commit half is a host serialization obligation, recorded out-of-scope (not over-claimed).
- Twin discipline: two isolated buggy twins, one per load-bearing invariant (`invitation-buggy.tla` → Inv 2; `invitation-buggy-derivation.tla` → Inv 12 positive derivation). Both auto-discovered and required-to-reject by `audit.mjs`.
- Result: **both load-bearing formal-layer claims (Inv 2, Inv 12) covered by named, twin-rejected checks; the FC F1 (bound saturation) and FC F2 (vacuous Inv 12) findings are closed; the FC F3 over-claim is corrected (single-resolution is post-write immutability, concurrent check-and-commit is a recorded runtime obligation).** Regenerated against the post-refactor model, 2026-06-23.
